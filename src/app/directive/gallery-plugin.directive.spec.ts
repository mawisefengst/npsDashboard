import { GalleryPluginDirective } from './gallery-plugin.directive';

describe('GalleryPluginDirective', () => {
  it('should create an instance', () => {
    const directive = new GalleryPluginDirective();
    expect(directive).toBeTruthy();
  });
});
